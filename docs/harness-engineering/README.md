# Harness Engineering — Research & Planning Index

## `assistant-ui-migration/`
Planning and execution tracking for evaluating (and ultimately rolling back)
`@assistant-ui/react` as Crystal's chat UI on this branch. See `TRACKER.md` for the
full history, including the decision to retire the assistant-ui chassis entirely and
keep only the CrystalOS harness improvements plus the legacy chat panel (this branch).
